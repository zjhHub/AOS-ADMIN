<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="RconciliationBillDownloadDao">
	
	<!-- 根据条件获取对账文件 -->
	<select id="getRconciliationBillFile" parameterType="Dto" resultType="String">
		
		select 
		A.file_path_
		from fc_platform_reconciliation as A
		left join fc_platform as B on A.platform_id_ = B.id_
		WHERE
		A.type_ = #{type_}
		and A.reconciliation_date_ = #{reconciliation_date_}
		and B.name_ = #{name_}
	
	</select> 
	
	
</mapper>