<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- -->
<mapper namespace="financialCategory">
	<select id="financialCategoryPage" parameterType="Dto"
		resultType="Dto">
		SELECT
			ffc.id_,
			ffc.name_,
			CASE WHEN ffc.type_= '1' THEN '冲销' WHEN ffc.type_= '2' THEN '截留' ELSE '其他' END AS type_,
			ffc.enabled_,
			ffc.remark_
		FROM
			fc_financial_category ffc
		WHERE 1=1
		
		<if test="name_ != null and name_ != ''">
			AND ffc.name_ like '%${name_}%'
		</if>
		
		<if test="type_ != null and type_ != ''">
			AND ffc.type_ = #{type_} <!-- 订单号 -->
		</if>
		
	</select>

</mapper>