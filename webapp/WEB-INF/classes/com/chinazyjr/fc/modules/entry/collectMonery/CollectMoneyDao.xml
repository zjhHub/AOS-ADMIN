<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CollectMoneyDao">

	<!-- 根据主键修改数据持久化对象 -->
	<update id="updateBookSubjectByKey" parameterType="Fc_book_subjectPO">
		UPDATE fc_book_subject
		<set>

			<if test="receivable_amount_ != null">
				receivable_amount_ = #{receivable_amount_, jdbcType=NUMERIC},  <!-- 应收金额 -->
			</if>
			<if test="received_amount_ != null">
				received_amount_ = #{received_amount_, jdbcType=NUMERIC},  <!-- 已收金额 -->
			</if>
			version_ = #{version_, jdbcType=INTEGER}+1,  <!-- version_ -->
		</set>
		WHERE id_ = #{id_} and version_=#{version_}
	</update>



	<!-- 根据主键修改数据持久化对象 -->
	<update id="updateBookCutByKey" parameterType="Fc_book_cutsPO">
		UPDATE fc_book_cuts
		<set>
			<if test="cut_amount_ != null">
				cut_amount_ = #{cut_amount_, jdbcType=NUMERIC},  <!-- 截留金额 -->
			</if>

			version_ = #{version_, jdbcType=INTEGER}+1,  <!-- version -->

		</set>
		WHERE id_ = #{id_} and version_=#{version_}
	</update>


</mapper>