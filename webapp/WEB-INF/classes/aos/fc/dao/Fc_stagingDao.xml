<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- fc_staging[fc_staging]SQL自动映射 -->
<!--  注意:此文件由AOS平台自动生成-禁止手工修改 2017-02-07 10:53:02 -->
<mapper namespace="aos.fc.dao.Fc_stagingDao">
	
	<!-- 插入一个数据持久化对象(插入字段为传入PO实体的非空属性) -->
	<insert id="insert" useGeneratedKeys="true" keyProperty="id_" parameterType="Fc_stagingPO">
		INSERT INTO fc_staging (
		<if test="contract_id_ != null">
	         contract_id_, <!-- 所属借款合同ID -->
		</if>
		<if test="no_ != null">
	         no_, <!-- 分期编号 -->
		</if>
		<if test="billing_date_ != null and billing_date_ != ''">
	         billing_date_, <!-- 分期账单日 -->
		</if>
		<if test="status_ != null">
	         status_, <!-- 分期状态，有3种：
            1、未到期
            2、已逾期
            3、已结清 -->
		</if>
	          id_
		)VALUES(
		<if test="contract_id_ != null">
	          #{contract_id_, jdbcType=INTEGER}, <!-- 所属借款合同ID -->
	    </if>
		<if test="no_ != null">
	          #{no_, jdbcType=INTEGER}, <!-- 分期编号 -->
	    </if>
		<if test="billing_date_ != null and billing_date_ != ''">
	          #{billing_date_, jdbcType=VARCHAR}, <!-- 分期账单日 -->
	    </if>
		<if test="status_ != null">
	          #{status_, jdbcType=INTEGER}, <!-- 分期状态，有3种：
            1、未到期
            2、已逾期
            3、已结清 -->
	    </if>
	          null
		)
	</insert>
	
	<!-- 插入一个数据持久化对象(含所有字段) -->
	<insert id="insertAll" useGeneratedKeys="true" keyProperty="id_" parameterType="Fc_stagingPO">
		INSERT INTO fc_staging (
		    <include refid="column" />
		)
		VALUES (
		    #{id_, jdbcType=INTEGER},  <!-- 分期ID -->
		    #{contract_id_, jdbcType=INTEGER},  <!-- 所属借款合同ID -->
		    #{no_, jdbcType=INTEGER},  <!-- 分期编号 -->
		    #{billing_date_, jdbcType=VARCHAR},  <!-- 分期账单日 -->
		    #{status_, jdbcType=INTEGER}  <!-- 分期状态，有3种：
            1、未到期
            2、已逾期
            3、已结清 -->
		)
	</insert>

	<!-- 根据主键修改数据持久化对象 -->
	<update id="updateByKey" parameterType="Fc_stagingPO">
		UPDATE fc_staging
		<set>
			<if test="id_ != null">
		          id_ = #{id_, jdbcType=INTEGER},  <!-- 分期ID -->
			</if>
			<if test="contract_id_ != null">
		          contract_id_ = #{contract_id_, jdbcType=INTEGER},  <!-- 所属借款合同ID -->
			</if>
			<if test="no_ != null">
		          no_ = #{no_, jdbcType=INTEGER},  <!-- 分期编号 -->
			</if>
			<if test="billing_date_ != null">
		          billing_date_ = #{billing_date_, jdbcType=VARCHAR},  <!-- 分期账单日 -->
			</if>
			<if test="status_ != null">
		          status_ = #{status_, jdbcType=INTEGER},  <!-- 分期状态，有3种：
            1、未到期
            2、已逾期
            3、已结清 -->
			</if>
		</set>
		WHERE  id_ = #{id_}
	</update>
	
	<!-- 根据主键删除数据持久化对象 -->
	<delete id="deleteByKey">
		DELETE FROM fc_staging WHERE  id_ = #{id_}
	</delete>

	<!-- 根据主键查询并返回数据持久化对象 -->
	<select id="selectByKey" resultType="Fc_stagingPO">
		SELECT
		<include refid="column" />
		FROM fc_staging WHERE  id_ = #{id_}
	</select>

	<!-- 根据唯一组合条件查询并返回数据持久化对象 -->
	<select id="selectOne" parameterType="Dto" resultType="Fc_stagingPO">
		SELECT
		    <include refid="column" />
		FROM fc_staging
		<where>
		    <include refid="equal" />
		</where>		
	</select>

	<!-- 根据Dto查询并返回数据持久化对象集合 -->
	<select id="list" parameterType="Dto" resultType="Fc_stagingPO">
		SELECT
			<include refid="column" />	
		FROM fc_staging
		<where>
		    <include refid="equal" />
		</where>	
	</select>

	<!-- 根据Dto查询并返回分页数据持久化对象集合 -->
	<select id="listPage" parameterType="Dto" resultType="Fc_stagingPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM fc_staging
		<where>
		    <include refid="equal" />
		</where>	
	</select>
	
	<!-- 根据Dto模糊查询并返回数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配) -->
	<select id="like" parameterType="Dto" resultType="Fc_stagingPO">
		SELECT
			<include refid="column" />	
		FROM fc_staging
		<where>
		    <include refid="like" />
		</where>	
	</select>

	<!-- 根据Dto模糊查询并返回分页数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配) -->
	<select id="likePage" parameterType="Dto" resultType="Fc_stagingPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM fc_staging
		<where>
		    <include refid="like" />
		</where>	
	</select>
	
	<!-- 根据Dto统计行数 -->
	<select id="rows" resultType="Integer" parameterType="Dto">
		SELECT COUNT(1) AS aos_rows_ FROM fc_staging
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 根据数学表达式进行数学运算 -->
	<select id="calc" parameterType="Dto" resultType="String">
		SELECT
			${_expr}
		FROM fc_staging
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 表字段 -->
	<sql id="column">
		  id_,  <!-- 分期ID -->
		  contract_id_,  <!-- 所属借款合同ID -->
		  no_,  <!-- 分期编号 -->
		  billing_date_,  <!-- 分期账单日 -->
		  status_  <!-- 分期状态，有3种：
            1、未到期
            2、已逾期
            3、已结清 -->
	</sql>
	
	<!-- 表字段(用于外表的关联时引用) -->
	<sql id="column2">
		  fc_staging.id_,  <!-- 分期ID -->
		  fc_staging.contract_id_,  <!-- 所属借款合同ID -->
		  fc_staging.no_,  <!-- 分期编号 -->
		  fc_staging.billing_date_,  <!-- 分期账单日 -->
		  fc_staging.status_  <!-- 分期状态，有3种：
            1、未到期
            2、已逾期
            3、已结清 -->
	</sql>

	<!-- Where精确匹配字段 -->
	<sql id="equal">
	   <if test="id_ != null ">
		      AND id_ = #{id_}  <!-- 分期ID -->
		</if>
	   <if test="contract_id_ != null ">
		      AND contract_id_ = #{contract_id_}  <!-- 所属借款合同ID -->
		</if>
	   <if test="no_ != null ">
		      AND no_ = #{no_}  <!-- 分期编号 -->
		</if>
	    <if test="billing_date_ != null and billing_date_ != ''">
		      AND billing_date_ = #{billing_date_}  <!-- 分期账单日 -->
		</if>
	   <if test="status_ != null ">
		      AND status_ = #{status_}  <!-- 分期状态，有3种：
            1、未到期
            2、已逾期
            3、已结清 -->
		</if>
	</sql>
	
	<!-- Where模糊匹配字段 -->
	<sql id="like">
	    <if test="billing_date_ != null and billing_date_ != ''">
		      AND billing_date_ like '%${billing_date_}%'  <!-- 分期账单日 -->
		</if>
	</sql>
	
</mapper>