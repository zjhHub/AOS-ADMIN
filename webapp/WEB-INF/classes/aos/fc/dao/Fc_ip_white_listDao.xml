<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- fc_ip_white_list[fc_ip_white_list]SQL自动映射 -->
<!--  注意:此文件由AOS平台自动生成-禁止手工修改 2017-02-07 10:53:00 -->
<mapper namespace="aos.fc.dao.Fc_ip_white_listDao">
	
	<!-- 插入一个数据持久化对象(插入字段为传入PO实体的非空属性) -->
	<insert id="insert" useGeneratedKeys="true" keyProperty="id_" parameterType="Fc_ip_white_listPO">
		INSERT INTO fc_ip_white_list (
		<if test="platform_id_ != null">
	         platform_id_, <!-- platform_id_ -->
		</if>
		<if test="ip_address_ != null and ip_address_ != ''">
	         ip_address_, <!-- IP地址 -->
		</if>
		<if test="deleted_ != null">
	         deleted_, <!-- deleted_ -->
		</if>
	          id_
		)VALUES(
		<if test="platform_id_ != null">
	          #{platform_id_, jdbcType=INTEGER}, <!-- platform_id_ -->
	    </if>
		<if test="ip_address_ != null and ip_address_ != ''">
	          #{ip_address_, jdbcType=VARCHAR}, <!-- IP地址 -->
	    </if>
		<if test="deleted_ != null">
	          #{deleted_, jdbcType=INTEGER}, <!-- deleted_ -->
	    </if>
	          null
		)
	</insert>
	
	<!-- 插入一个数据持久化对象(含所有字段) -->
	<insert id="insertAll" useGeneratedKeys="true" keyProperty="id_" parameterType="Fc_ip_white_listPO">
		INSERT INTO fc_ip_white_list (
		    <include refid="column" />
		)
		VALUES (
		    #{id_, jdbcType=INTEGER},  <!-- 白名单ID -->
		    #{platform_id_, jdbcType=INTEGER},  <!-- platform_id_ -->
		    #{ip_address_, jdbcType=VARCHAR},  <!-- IP地址 -->
		    #{deleted_, jdbcType=INTEGER}  <!-- deleted_ -->
		)
	</insert>

	<!-- 根据主键修改数据持久化对象 -->
	<update id="updateByKey" parameterType="Fc_ip_white_listPO">
		UPDATE fc_ip_white_list
		<set>
			<if test="id_ != null">
		          id_ = #{id_, jdbcType=INTEGER},  <!-- 白名单ID -->
			</if>
			<if test="platform_id_ != null">
		          platform_id_ = #{platform_id_, jdbcType=INTEGER},  <!-- platform_id_ -->
			</if>
			<if test="ip_address_ != null">
		          ip_address_ = #{ip_address_, jdbcType=VARCHAR},  <!-- IP地址 -->
			</if>
			<if test="deleted_ != null">
		          deleted_ = #{deleted_, jdbcType=INTEGER},  <!-- deleted_ -->
			</if>
		</set>
		WHERE  id_ = #{id_}
	</update>
	
	<!-- 根据主键删除数据持久化对象 -->
	<delete id="deleteByKey">
		DELETE FROM fc_ip_white_list WHERE  id_ = #{id_}
	</delete>

	<!-- 根据主键查询并返回数据持久化对象 -->
	<select id="selectByKey" resultType="Fc_ip_white_listPO">
		SELECT
		<include refid="column" />
		FROM fc_ip_white_list WHERE  id_ = #{id_}
	</select>

	<!-- 根据唯一组合条件查询并返回数据持久化对象 -->
	<select id="selectOne" parameterType="Dto" resultType="Fc_ip_white_listPO">
		SELECT
		    <include refid="column" />
		FROM fc_ip_white_list
		<where>
		    <include refid="equal" />
		</where>		
	</select>

	<!-- 根据Dto查询并返回数据持久化对象集合 -->
	<select id="list" parameterType="Dto" resultType="Fc_ip_white_listPO">
		SELECT
			<include refid="column" />	
		FROM fc_ip_white_list
		<where>
		    <include refid="equal" />
		</where>	
	</select>

	<!-- 根据Dto查询并返回分页数据持久化对象集合 -->
	<select id="listPage" parameterType="Dto" resultType="Fc_ip_white_listPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM fc_ip_white_list
		<where>
		    <include refid="equal" />
		</where>	
	</select>
	
	<!-- 根据Dto模糊查询并返回数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配) -->
	<select id="like" parameterType="Dto" resultType="Fc_ip_white_listPO">
		SELECT
			<include refid="column" />	
		FROM fc_ip_white_list
		<where>
		    <include refid="like" />
		</where>	
	</select>

	<!-- 根据Dto模糊查询并返回分页数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配) -->
	<select id="likePage" parameterType="Dto" resultType="Fc_ip_white_listPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM fc_ip_white_list
		<where>
		    <include refid="like" />
		</where>	
	</select>
	
	<!-- 根据Dto统计行数 -->
	<select id="rows" resultType="Integer" parameterType="Dto">
		SELECT COUNT(1) AS aos_rows_ FROM fc_ip_white_list
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 根据数学表达式进行数学运算 -->
	<select id="calc" parameterType="Dto" resultType="String">
		SELECT
			${_expr}
		FROM fc_ip_white_list
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 表字段 -->
	<sql id="column">
		  id_,  <!-- 白名单ID -->
		  platform_id_,  <!-- platform_id_ -->
		  ip_address_,  <!-- IP地址 -->
		  deleted_  <!-- deleted_ -->
	</sql>
	
	<!-- 表字段(用于外表的关联时引用) -->
	<sql id="column2">
		  fc_ip_white_list.id_,  <!-- 白名单ID -->
		  fc_ip_white_list.platform_id_,  <!-- platform_id_ -->
		  fc_ip_white_list.ip_address_,  <!-- IP地址 -->
		  fc_ip_white_list.deleted_  <!-- deleted_ -->
	</sql>

	<!-- Where精确匹配字段 -->
	<sql id="equal">
	   <if test="id_ != null ">
		      AND id_ = #{id_}  <!-- 白名单ID -->
		</if>
	   <if test="platform_id_ != null ">
		      AND platform_id_ = #{platform_id_}  <!-- platform_id_ -->
		</if>
	    <if test="ip_address_ != null and ip_address_ != ''">
		      AND ip_address_ = #{ip_address_}  <!-- IP地址 -->
		</if>
	   <if test="deleted_ != null ">
		      AND deleted_ = #{deleted_}  <!-- deleted_ -->
		</if>
	</sql>
	
	<!-- Where模糊匹配字段 -->
	<sql id="like">
	    <if test="ip_address_ != null and ip_address_ != ''">
		      AND ip_address_ like '%${ip_address_}%'  <!-- IP地址 -->
		</if>
	</sql>
	
</mapper>